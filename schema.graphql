type BetPlacedEvent @entity {
  id: ID!                # 唯一 ID (transaction hash + log index)
  gameId: BigInt!
  player: Bytes!
  orderId: BigInt!
  amount: BigInt!
  hash: String!
  blockNumber: BigInt!
  timestamp: BigInt!
  txHash: Bytes!
}
